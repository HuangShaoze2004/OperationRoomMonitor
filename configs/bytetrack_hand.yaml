# 接触判定阶段手部 ByteTrack（基于 ultralytics 默认模板，可调 track_buffer）
tracker_type: bytetrack
track_high_thresh: 0.5
track_low_thresh: 0.1
new_track_thresh: 0.6
track_buffer: 30
match_thresh: 0.8
fuse_score: true
